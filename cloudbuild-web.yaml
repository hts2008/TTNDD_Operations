steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/ttndd-web:latest'
      - '--build-arg'
      - 'NEXT_PUBLIC_API_URL=https://ttndd-api-122940795437.asia-southeast1.run.app/api/v1'
      - '-f'
      - 'apps/web/Dockerfile'
      - '.'
    id: build-web

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/ttndd-web:latest'
    id: push-web
    waitFor: ['build-web']

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'ttndd-platform'
      - '--image=gcr.io/$PROJECT_ID/ttndd-web:latest'
      - '--region=asia-southeast1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--port=3000'
      - '--memory=256Mi'
      - '--cpu=1'
      - '--min-instances=0'
      - '--max-instances=5'
    id: deploy-web
    waitFor: ['push-web']

images:
  - 'gcr.io/$PROJECT_ID/ttndd-web:latest'

timeout: '600s'
